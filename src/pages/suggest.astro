---
import Layout from '../layouts/Layout.astro'

import Header from '../components/Header.vue'
import Footer from '../components/Footer.vue'

import SuggestView from '../components/SuggestView.vue'

import IconSvg from '../img/icon.svg'
---

<Layout>
	<main class="h-screen flex flex-col justify-between gap-0">
		<!-- Application Header -->
		<Header client:load />

		<section class="w-full lg:max-w-5xl mx-auto p-3 sm:p-8 flex-1 flex-col overflow-y-scroll overflow-x-hidden">

			<SuggestView client:load />

		</section>

		<!-- Application Footer -->
		<Footer client:load />
	</main>
</Layout>

<script>
console.log('PaynBits SUGGEST is starting up...')

/* Import module.s */
import { sdk } from '@farcaster/miniapp-sdk'

/* Prepare UI for interactions. */
await sdk.actions.ready()

console.log('PaynBits SUGGEST has been initialized!')
</script>
